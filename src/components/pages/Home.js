import React from 'react';
import Navbar from '../Navbar/navbar';
import Footer from '../Footer/footer';

const Home = (props) => {
  return (
    <div className="Home">
      <Navbar />
      <Footer />
    </div>
  );
};

export default Home;
