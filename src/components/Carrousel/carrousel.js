import React from 'react';
import './carrousel.scss';

const Carrousel = () => {
  return (
    <div className="tempCarrouselPic">
      <a href="#">Make Shift</a>
    </div>
  );
};

export default Carrousel;
