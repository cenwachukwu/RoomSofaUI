import React from 'react';
import './Sidebar.scss';

const Sidebar = (props) => {
  return (
    <div className="Sidebar">
      <p>we are the Sidebar page</p>
    </div>
  );
};

export default Sidebar;
